package org.example.testapp;

import org.qtproject.qt.android.bindings.QtActivity;

public class MainActivity extends QtActivity {
    // Qt gère tout. Ce fichier est requis pour que l'application Android démarre.
}
